import { redirect } from 'next/navigation';

/** ARCHIVED: legacy React cases pipeline. Live UI lives in LiveDemoPage iframe. */
export default function ArchivedCasesPage() {
  redirect('/dashboard');
}
