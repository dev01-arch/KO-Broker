import { redirect } from 'next/navigation';

/** ARCHIVED: legacy React AI reports page. Live UI lives in LiveDemoPage iframe. */
export default function ArchivedAiReportsPage() {
  redirect('/dashboard');
}
