import { redirect } from 'next/navigation';

/** ARCHIVED: legacy React client detail. Live UI lives in LiveDemoPage iframe. */
export default function ArchivedClientDetailPage() {
  redirect('/dashboard');
}
