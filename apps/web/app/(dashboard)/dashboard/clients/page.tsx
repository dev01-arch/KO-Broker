import { redirect } from 'next/navigation';

/** ARCHIVED: legacy React clients table. Live UI lives in LiveDemoPage iframe. */
export default function ArchivedClientsPage() {
  redirect('/dashboard');
}
