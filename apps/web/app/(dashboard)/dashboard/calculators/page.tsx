import { redirect } from 'next/navigation';

/** ARCHIVED: legacy React calculators stub. Live UI lives in LiveDemoPage. */
export default function ArchivedCalculatorsPage() {
  redirect('/dashboard');
}
