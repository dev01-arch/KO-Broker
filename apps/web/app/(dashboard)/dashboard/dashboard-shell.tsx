/**
 * ARCHIVED — unused legacy dashboard shell (sidebar + section routes).
 * The approved dashboard is LiveDemoPage (iframe). Do not remount this shell.
 * Full prior implementation is preserved in git history.
 */
export function DashboardShell({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
