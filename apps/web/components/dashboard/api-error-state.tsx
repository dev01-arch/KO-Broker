/**
 * ARCHIVED — legacy error UI for old React dashboard section pages.
 * Full prior implementation is preserved in git history.
 */
export function ApiErrorState(_props: {
  title?: string;
  message?: string;
  onRetry?: () => void;
  className?: string;
}) {
  return null;
}
