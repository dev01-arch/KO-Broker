/**
 * ARCHIVED — legacy "Add client" modal for the old React clients table.
 * Client creation now runs inside the LiveDemoPage iframe drawer.
 * Full prior implementation is preserved in git history.
 */
export function AddClientModal(_props: { open: boolean; onClose: () => void }) {
  return null;
}
