/**
 * ARCHIVED — legacy React dashboard sidebar.
 * The approved dashboard is LiveDemoPage (iframe). Do not remount this nav.
 * Full prior implementation is preserved in git history.
 */
export function DashboardNav({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
