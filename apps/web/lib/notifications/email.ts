/**
 * Email delivery service — PRD-10
 *
 * Uses Resend for transactional email delivery.
 * Called by notification triggers (compliance stage advance,
 * AI report approval, case updates).
 */

// TODO (PRD-10): Install resend, implement sendEmail()

export {};
